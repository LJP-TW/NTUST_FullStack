import { ConfirmPwdDirective } from './confirm-pwd.directive';

describe('ConfirmPwdDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmPwdDirective();
    expect(directive).toBeTruthy();
  });
});
